{
  "name": "git-code",
  "version": "0.0.1-alpha",
  "private": true,
  "description": "Easily get a list of repositories and open them in GitHub or vscode.",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "files": [
    "bin/",
    "lib/",
    "public/"
  ],
  "bin": {
    "gcode": "bin/index.js"
  },
  "scripts": {
    "start": "node scripts/cleanup.mjs && tsc && node bin",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "lightzane",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "typescript": "^5.3.3"
  }
}
